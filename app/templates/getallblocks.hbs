<div class="jumbotron">
  <div class="container">
	<p class="lead">Pool always pay full block reward including TX fees and uncle rewards.</p>
	It may take some time to confirm the generated block. Usually this happens rather quickly.<br>
	<strong>
      Block maturity requires <u>up to</u> </strong> <span class="label label-success">520</span> <strong>blocks;
      Generally, it's less than this number.
    </strong>
  </div>
</div>
<div class="container">
  {{#if model.luck}}
    {{partial "luck"}}
  {{/if}}
  <ul class="nav nav-tabs">
    {{#active-li currentWhen='getallblocks.index' role='presentation'}}
      {{#link-to 'getallblocks.index'}}Mature blocks <span class="badge alert-success">{{format-number model.maturedTotal}}</span>{{/link-to}}
    {{/active-li}}
    {{#active-li currentWhen='getallblocks.immature' role='presentation'}}
      {{#link-to 'getallblocks.immature'}}Immature blocks <span class="badge alert-success">{{format-number model.immatureTotal}}</span>{{/link-to}}
    {{/active-li}}
    {{#active-li currentWhen='getallblocks.pending' role='presentation'}}
      {{#link-to 'getallblocks.pending'}}New blocks <span class="badge alert-info">{{format-number model.candidatesTotal}}</span>{{/link-to}}
    {{/active-li}}
  </ul>
  {{outlet}}
</div>